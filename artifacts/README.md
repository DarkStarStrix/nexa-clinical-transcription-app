# Artifacts

This folder is for local build outputs.

- `app-bounty-debug.apk`: debug APK built with the `bounty` flavor.

Note: Git hosting providers often reject APKs larger than 100MB. For sharing, upload the APK as a **GitHub Release asset** and link it from the main `README.md`.

